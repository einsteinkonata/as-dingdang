package Konata.Panels 
{
	import KO.KEmitter;
	import KO.KEntity;
	
	public class EmitterPanel extends KEntity
	{
		public function EmitterPanel()
		{
			//emitter = new KEmitter(this.renderData.clone(), this.diameter, this.diameter);
			//emitter.newType("a", [0]);
			//emitter.setAlpha("a", .6, 0);
		}
	}
}